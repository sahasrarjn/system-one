[project]
name = "systemone"
version = "0.1.0"
description = "A calibrated typed-decision model on an open backbone"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["systemone*"]
